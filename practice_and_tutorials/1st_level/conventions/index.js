// JS Conventions

// casing - camelCased: Capitalizing the first letter of every word except the first.

var myLocationIsInUtah

// white space - JavaScript doesn't read white space, so put spaces in your syntax to make it easier to read

for(var i = 0; i < 10; i++) {

}

// naming - Using common sense to name your variables and functions what they are, to make them easier to identify

var firstName = "Erik"
var lastName = "Tycksen"

var friends = []
var isDone = truevar 
var isGameOver = false

function sum(num1, num2) {

}

// bracket pair colorizor
// braces

var person = {
    address: {

    }
}

// tab (indenting)

if (2 === 2) {
    console.log ("they are equal")
    if (3 ===3) {
        console.log ("They are equal")
    }
}

// HTML tags

    // Don't make them up, always lowercase, use existing tags like div and span.

// comments

    /*
    anything between these are commented out
    */

// unix casing

    // silly cia - no
    // Silly Cia - no
    //sillycia - yes